export class ConversationProvider {

    public getResponse(input: string): string {
        // consider leveraging https://github.com/fancy-flashcard/fancy-nlp
        return "Go with the flow."
    }

}